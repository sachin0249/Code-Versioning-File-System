# Code-Versioning-File-System
This program simulates a code versioning and file system using a Binary Search Tree. Each node stores a unique version ID, timestamp, and project name. The system supports committing new versions, checking out a specific version, and rolling back obsolete versions while maintaining BST properties for efficient access and deletion.
